# Core — pinned to the versions this project was developed and trained on.
# For CUDA builds of torch/torchvision, install from https://pytorch.org first.
torch==2.10.0
torchvision==0.25.0

numpy==2.3.5
scipy==1.17.0
h5py==3.15.1
Pillow==12.0.0
matplotlib==3.10.8
tqdm==4.67.3

# Optional — only needed for the Ollama-backed XAI module (xai_ollama.py)
requests==2.32.5
